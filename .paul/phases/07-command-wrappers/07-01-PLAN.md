---
phase: 07-command-wrappers
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - apps/base/src/commands/pulse.md
  - apps/base/src/commands/groom.md
  - apps/base/src/commands/audit.md
  - apps/base/src/commands/audit-claude-md.md
  - apps/base/src/commands/scaffold.md
  - apps/base/src/commands/status.md
  - apps/base/src/commands/history.md
  - apps/base/src/commands/carl-hygiene.md
  - ~/.claude/commands/base/ (symlinked)
  - .claude/skills/base/ (symlinked)
autonomous: true
---

<objective>
## Goal
Create 8 command wrappers for all existing BASE commands directly in apps/base/src/commands/ (source of truth), then symlink workspace install locations to apps/base/src/ so Chris's workspace always runs latest.

## Purpose
Only 3 surface commands have slash command wrappers. The 8 core BASE commands (pulse, groom, audit, scaffold, status, history, audit-claude-md, carl-hygiene) are only accessible through the skill entry point. They need proper /base:{name} slash commands. Symlinks eliminate the copy-sync problem for the developer.

## Output
- 8 new command wrappers in apps/base/src/commands/ (11 total with existing 3)
- ~/.claude/commands/base/ symlinked to apps/base/src/commands/
- .claude/skills/base/ symlinked to apps/base/src/skill/
- .base/hooks/ files symlinked to apps/base/src/hooks/ (individual files)
</objective>

<context>
## Reference
@~/.claude/commands/base/surface-create.md — Existing wrapper pattern to follow exactly

## Task files that need wrappers
@.claude/skills/base/tasks/pulse.md
@.claude/skills/base/tasks/groom.md
@.claude/skills/base/tasks/audit.md
@.claude/skills/base/tasks/audit-claude-md.md
@.claude/skills/base/tasks/scaffold.md
@.claude/skills/base/tasks/status.md
@.claude/skills/base/tasks/history.md
@.claude/skills/base/tasks/carl-hygiene.md
</context>

<acceptance_criteria>

## AC-1: All 11 command wrappers exist in apps/base/src/commands/
```gherkin
Given apps/base/src/commands/ has 3 surface command wrappers
When 8 new command wrappers are created
Then apps/base/src/commands/ contains 11 .md files
And each follows the surface-create.md pattern (frontmatter, objective, execution_context, context, process, success_criteria)
```

## AC-2: Symlinks replace real directories for developer workflow
```gherkin
Given real directories exist at ~/.claude/commands/base/ and .claude/skills/base/
When symlinks are set up
Then ~/.claude/commands/base/ is a symlink to apps/base/src/commands/
And .claude/skills/base/ is a symlink to apps/base/src/skill/
And .base/hooks/ contains symlinks to individual files in apps/base/src/hooks/
And all 11 commands appear in Claude Code skill list
```

## AC-3: Existing functionality preserved
```gherkin
Given the workspace has working surfaces, hooks, and MCP tools
When symlinks replace real directories
Then active-awareness and backlog-awareness hooks still fire
Then BASE MCP tools still respond
Then existing surface commands still appear in skill list
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create 8 command wrappers in apps/base/src/commands/</name>
  <files>
    apps/base/src/commands/pulse.md,
    apps/base/src/commands/groom.md,
    apps/base/src/commands/audit.md,
    apps/base/src/commands/audit-claude-md.md,
    apps/base/src/commands/scaffold.md,
    apps/base/src/commands/status.md,
    apps/base/src/commands/history.md,
    apps/base/src/commands/carl-hygiene.md
  </files>
  <action>
    Create 8 command wrapper files following the EXACT pattern from surface-create.md:

    Each wrapper has:
    - Frontmatter: name (base:{command}), description, allowed-tools
    - objective: One-line description + when to use
    - execution_context: @.claude/skills/base/tasks/{command}.md + any context files
    - context: $ARGUMENTS + relevant @-references (.base/workspace.json, .base/STATE.md as appropriate)
    - process: "Follow task: @.claude/skills/base/tasks/{command}.md"
    - success_criteria: Checklist specific to the command

    **Command-specific details:**

    1. pulse.md — Daily health briefing
       - allowed-tools: [Read, Glob, Grep, Bash]
       - context: @.base/workspace.json, @.base/STATE.md

    2. groom.md — Weekly maintenance cycle
       - allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion]
       - context: @.base/workspace.json, @.base/STATE.md

    3. audit.md — Deep workspace optimization
       - allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Agent, AskUserQuestion]
       - context: @.base/workspace.json

    4. audit-claude-md.md — CLAUDE.md audit and generation
       - allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]
       - context: @CLAUDE.md

    5. scaffold.md — Set up BASE in new workspace
       - allowed-tools: [Read, Write, Edit, Glob, Bash, AskUserQuestion]
       - context: $ARGUMENTS

    6. status.md — Quick health check
       - allowed-tools: [Read, Glob, Bash]
       - context: @.base/workspace.json, @.base/STATE.md

    7. history.md — Workspace evolution timeline
       - allowed-tools: [Read, Glob, Bash]
       - context: @.base/STATE.md

    8. carl-hygiene.md — CARL domain maintenance
       - allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion]
       - context: @.carl/manifest, @.base/workspace.json

    Write ALL 8 files directly to apps/base/src/commands/ (the source of truth).
    Do NOT write to ~/.claude/commands/base/ — that will be symlinked.
  </action>
  <verify>
    - find apps/base/src/commands/ -name "*.md" | wc -l returns 11
    - Each new file has frontmatter with name: base:{command}
    - grep "Follow task:" apps/base/src/commands/*.md returns 11 matches
  </verify>
  <done>AC-1 satisfied: All 11 command wrappers in apps/base/src/commands/</done>
</task>

<task type="auto">
  <name>Task 2: Set up symlinks from workspace to apps/base/src/</name>
  <files>~/.claude/commands/base/, .claude/skills/base/, .base/hooks/</files>
  <action>
    **1. Commands symlink:**
    - Remove existing ~/.claude/commands/base/ directory (has 3 real files)
    - Create symlink: ~/.claude/commands/base → /home/chriskahler/chris-ai-systems/apps/base/src/commands
    - Verify: ls -la ~/.claude/commands/base/ shows all 11 wrappers

    **2. Skill symlink:**
    - Remove existing .claude/skills/base/ directory (has real files — already copied to apps/base/src/skill/)
    - Create symlink: .claude/skills/base → /home/chriskahler/chris-ai-systems/apps/base/src/skill
    - Verify: ls -la .claude/skills/base/ shows base.md, tasks/, templates/, etc.

    **3. Hooks symlinks (individual files, not directory):**
    - .base/hooks/ directory stays real (needs to be a real dir for new hooks)
    - Remove real files: _template.py, active-hook.py, backlog-hook.py
    - Create symlinks for each:
      - .base/hooks/_template.py → apps/base/src/hooks/_template.py
      - .base/hooks/active-hook.py → apps/base/src/hooks/active-hook.py
      - .base/hooks/backlog-hook.py → apps/base/src/hooks/backlog-hook.py
    - Session hooks (.claude/hooks/base-pulse-check.py, psmm-injector.py) stay as real files
      (they use absolute paths in settings.json — symlinking would break path resolution)

    **4. Verify nothing broke:**
    - echo '{"session_id":"test"}' | python3 .base/hooks/active-hook.py produces output
    - echo '{"session_id":"test"}' | python3 .base/hooks/backlog-hook.py produces output

    Avoid: Symlinking .base/base-mcp/ or .base/carl-mcp/ (need real node_modules)
    Avoid: Symlinking .claude/hooks/ session hooks (absolute path resolution breaks)
    Avoid: Using relative symlinks — use absolute paths for reliability
  </action>
  <verify>
    - test -L ~/.claude/commands/base confirms symlink (not dir)
    - test -L .claude/skills/base confirms symlink
    - test -L .base/hooks/_template.py confirms symlink
    - test -L .base/hooks/active-hook.py confirms symlink
    - test -L .base/hooks/backlog-hook.py confirms symlink
    - ls ~/.claude/commands/base/ shows 11 .md files
    - ls .claude/skills/base/ shows base.md, tasks/, templates/, etc.
    - Hook test produces <active-awareness> output
  </verify>
  <done>AC-2, AC-3 satisfied: Symlinks in place, functionality preserved</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- apps/base/src/skill/ — already populated, don't modify task file content
- apps/base/src/hooks/ — already populated, don't modify hook content
- apps/base/src/packages/ — MCP servers, not symlinked
- .base/base-mcp/ — real install with node_modules
- .base/carl-mcp/ — real install with node_modules
- .base/data/ — workspace data, never symlinked
- .base/workspace.json — workspace config
- .claude/hooks/base-pulse-check.py — real file (absolute path in settings.json)
- .claude/hooks/psmm-injector.py — real file (absolute path in settings.json)
- .claude/settings.json — hook registrations stay as-is

## SCOPE LIMITS
- Command wrappers only — don't modify the underlying task files
- Symlinks for Chris's dev workflow only — installer still copies for other users
- No changes to bin/install.js or package.json
- No MCP server modifications

</boundaries>

<verification>
Before declaring plan complete:
- [ ] 11 command wrapper files in apps/base/src/commands/
- [ ] ~/.claude/commands/base is a symlink to apps/base/src/commands/
- [ ] .claude/skills/base is a symlink to apps/base/src/skill/
- [ ] .base/hooks/ has 3 symlinked files
- [ ] Hooks still produce correct output when piped test input
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 11 commands visible as /base:* slash commands (after session restart)
- Developer workflow: edit in apps/base/src/, workspace sees changes instantly
</success_criteria>

<output>
After completion, create `.paul/phases/07-command-wrappers/07-01-SUMMARY.md`
</output>
